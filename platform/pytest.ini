[pytest]
DJANGO_SETTINGS_MODULE = config.settings_test
python_files = test_*.py
pythonpath = .
addopts = -q
# platform/tests = kernel tests. ../modules = each module's own backend/tests,
# discovered here since there's one Django project/one pytest run for both.
testpaths = tests ../modules
