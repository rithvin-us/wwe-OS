# WWE OS platform kernel — runtime dependencies.
python-dotenv>=1.2.3    # loads repo-root .env for local (non-Docker) runs
Django>=6.1,<6.2
djangorestframework>=3.18.0
djangorestframework-simplejwt>=5.5.1
drf-spectacular>=0.30
django-cors-headers>=4.9
django-filter>=26.1
argon2-cffi>=25
dj-database-url>=3.1
psycopg[binary]>=3.2
redis>=5.2
gunicorn>=23.0
# Platform services
httpx>=0.28.1        # AI gateway provider calls
openpyxl>=3.1.5      # reporting: Excel renderer
reportlab>=5.0.0       # reporting: PDF renderer
pypdf>=5.0           # auditor: merges PDFs into one package
# storage/providers.py imports boto3 lazily, but production (and any dev env
# pointed at real S3/R2 storage) sets STORAGE_BACKEND=s3, so it's not truly
# optional — Render's build only runs this file, with no conditional install
# step, so leaving it out here means every upload 500s in production.
boto3>=1.43.78
docxtpl==0.20.2
pywebpush>=2.4.0      # notifications: Web Push delivery + VAPID signing
