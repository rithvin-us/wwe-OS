# WWE OS platform kernel — runtime dependencies.
Django>=6.0,<6.1
djangorestframework>=3.17
djangorestframework-simplejwt>=5.5
drf-spectacular>=0.30
django-cors-headers>=4.9
django-filter>=26
argon2-cffi>=25
dj-database-url>=3.1
psycopg[binary]>=3.2
redis>=5.2
gunicorn>=23.0
# Platform services
httpx>=0.28          # AI gateway provider calls
openpyxl>=3.1        # reporting: Excel renderer
reportlab>=4.2       # reporting: PDF renderer
# boto3 is intentionally NOT listed: install it only when STORAGE_BACKEND is
# s3/r2/minio (see storage/providers.py — the import is lazy).
