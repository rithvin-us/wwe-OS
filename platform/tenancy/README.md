# Platform · Tenancy

Multi-tenancy core: tenant lifecycle (provision, suspend, delete),
tenant resolution (domain/header/token), data isolation strategy,
per-tenant configuration and feature flags.

- Owns: the tenant aggregate and the isolation contract every query obeys.
