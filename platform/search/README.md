# Platform · Search

Cross-module indexing and search. Modules register searchable entities;
the platform handles indexing, tenant-scoped querying, ranking, and
permission-filtered results.

- Owns: index lifecycle, query API, result authorization.
