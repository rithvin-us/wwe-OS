# Package · @bop/theme

Theme runtime for the platform: one `ThemeProvider` (class-based dark mode,
system default) and the `useTheme` hook. Mounted once in each app's root layout.
Visual values live in `@bop/design-system` — this package only switches them.
