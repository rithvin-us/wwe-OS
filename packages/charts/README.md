# Package · @bop/charts

Themed chart layer over Recharts. Exposes `chartPalette` bound to
design-system chart tokens; all platform charts consume it. No hardcoded
colors, no per-module chart styles.
