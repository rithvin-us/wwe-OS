/**
 * Icon gate. Every icon in the platform is imported from here, never from
 * lucide-react directly. Keeps the icon vocabulary auditable and swappable.
 */
export * from "lucide-react";
export type { LucideIcon } from "lucide-react";
