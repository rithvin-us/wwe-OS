# Package · @bop/icons

Single gate for icons (Lucide). Apps and packages import icons from
`@bop/icons`, never from `lucide-react` directly — keeps the icon vocabulary
auditable and the library swappable. No emoji anywhere in product UI.
