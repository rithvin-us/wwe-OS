# Scripts

Developer and operations scripts: setup, database tasks, code generation,
release helpers. Every script must be idempotent and documented at the top of
the file. Nothing in here may contain business logic.
