/**
 * Company identity shown across the shell (sidebar, login, titles).
 * Edit this one file to rebrand the platform — nothing else references
 * company details directly.
 */
export const COMPANY = {
  name: "WWE OS",
  /** Short caption under the name. */
  caption: "Operations",
  /** 1–2 letters for the logo mark. */
  mark: "W",
};
